// Spec 84 §2.3 (Wave 3) — target route for the wizard's post-connect
// redirect: the dedicated AI settings page (apps/web/app/routes/_app.settings.ai.tsx).
// Every call site imports this constant instead of hardcoding the path.
export const SETTINGS_ROUTE_PATH = "/settings/ai";
