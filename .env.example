# This file is kept for backwards compatibility only.
# The canonical environment template is apps/web/.env.example —
# that is the single file Vinxi and all scripts read from.
#
# Run `cp apps/web/.env.example apps/web/.env` to get started.
