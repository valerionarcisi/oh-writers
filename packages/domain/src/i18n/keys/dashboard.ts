import type { LocaleDict } from "./types.js";

/** dashboard feature keys. Filled by the i18n bulk extraction (Spec 18b PR-5+). */
export const dashboardKeys = {
  en: {},
  it: {},
} as const satisfies LocaleDict;
