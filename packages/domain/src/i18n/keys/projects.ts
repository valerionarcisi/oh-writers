import type { LocaleDict } from "./types.js";

/** projects feature keys. Filled by the i18n bulk extraction (Spec 18b PR-5+). */
export const projectsKeys = {
  en: {},
  it: {},
} as const satisfies LocaleDict;
