import type { LocaleDict } from "./types.js";

/** budget feature keys. Filled by the i18n bulk extraction (Spec 18b PR-5+). */
export const budgetKeys = {
  en: {},
  it: {},
} as const satisfies LocaleDict;
