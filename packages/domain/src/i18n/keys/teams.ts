import type { LocaleDict } from "./types.js";

/** teams feature keys. Filled by the i18n bulk extraction (Spec 18b PR-5+). */
export const teamsKeys = {
  en: {},
  it: {},
} as const satisfies LocaleDict;
